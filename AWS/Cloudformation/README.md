# Cloudformation YAML Scripts

### Create a Cloudformation:
```
aws cloudformation create-stack --stack-name myTestStack --template-body file://AWS/Cloudformation/vpc_s3_ec2_image_viewer.yaml --capabilities CAPABILITY_IAM
```

